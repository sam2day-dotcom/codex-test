# Future Extension Guide

- Add modules under `includes/Services` for cross-cutting concerns.
- Premium-only blocks belong in `/pro` and integrate through feature registry.
- Updater can be extended with token auth for private GitHub repositories.
